#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *t5 = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", t5);
	return (0);
}
